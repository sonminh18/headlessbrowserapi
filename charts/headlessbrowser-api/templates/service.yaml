apiVersion: v1
kind: Service
metadata:
  name: {{ include "headlessbrowser-api.fullname" . }}
  labels:
    {{- include "headlessbrowser-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "headlessbrowser-api.selectorLabels" . | nindent 4 }}
